// Loads NPM managed packages as modules so that you can run and execute them. 
// mainly loads the tgz and applys the npm hooks and returns handels to the files in it. 
// additional it offers helpers to run scripts from the package.json and resolve
// the correct imports. for dependency loading this uses the ./unpkg.js modules.
